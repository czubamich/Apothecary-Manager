<catel:UserControl x:Class="ApothecaryManager.WPF.Views.StatusBarView"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:catel="http://schemas.catelproject.com"
                   xmlns:mahapps="http://metro.mahapps.com/winfx/xaml/controls">

    <StackPanel x:Name="stackPanel" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 0 4 4">
        <TextBlock Text="Receiving automatic updates: " />
        <TextBlock Text="{Binding EnableAutomaticUpdates}" />

        <Separator Margin="3,0,3,0" Width="{Binding ElementName=stackPanel, Path=ActualHeight}">
            <Separator.LayoutTransform>
                <RotateTransform Angle="90" />
            </Separator.LayoutTransform>
        </Separator>

        <TextBlock Text="CONNECTED" />
    </StackPanel>

</catel:UserControl>
